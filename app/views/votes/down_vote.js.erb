<% if @down_vote.valid? %>
  $<'.down-vote').html("<%= escape_javascript(render partial: 'votes/voter', locals: { post: post }) %>");
<% else %>
  $<'.down-vote').html("<%= escape_javascript(render partial: 'votes/voter', locals: { post: post }) %>");
<% end %>
